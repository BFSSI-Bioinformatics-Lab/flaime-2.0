import { styled } from "@mui/system";

export default styled('div')(() => ({
    ".MuiSlider-thumb": {
        transition: "none"
    },
    ".MuiSlider-track": {
        transition: "none"
    },
    "span": {
        transition: "none"
    }
}));
